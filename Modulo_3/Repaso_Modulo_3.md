Definicion de Lenguaje Python:
    Python es un lenguaje de programación de alto nivel, interpretado y de propósito general. Fue creado por Guido van Rossum y lanzado por primera vez en 1991. Python se caracteriza por su sintaxis clara y legible, lo que facilita su aprendizaje y uso. Es ampliamente utilizado en diversos campos, como desarrollo web, análisis de datos, inteligencia artificial, automatización, entre otros. Python cuenta con una gran comunidad de desarrolladores y una amplia variedad de bibliotecas y frameworks que amplían sus capacidades.

Sintasis de Python:
    - Tipado dinamico: No es necesario declarar el tipo de variable, ya que Python lo infiere automáticamente.

Variables en python:
    - Las variables se crean al asignar un valor a un nombre. Por ejemplo: `x = 5` crea una variable llamada `x` con el valor `5`.
funcion print():
    - La función `print()` se utiliza para mostrar información en la consola. Por ejemplo: `print("Hola, mundo!")` imprimirá "Hola, mundo!" en la consola.
Listas en Python:
    - Las listas son estructuras de datos que pueden contener múltiples elementos. Se definen utilizando corchetes `[]`. Por ejemplo: `mi_lista = [1, 2, 3, 4]` crea una lista con los números del 1 al 4.
Diccionarios en Python:
    - Los diccionarios son estructuras de datos que almacenan pares clave-valor. Se definen utilizando llaves `{}`. Por ejemplo: `mi_diccionario = {"nombre": "Juan", "edad": 30}` crea un diccionario con las claves "nombre" y "edad" y sus respectivos valores.
Operadores Aritmetricos:
- suma: `+`
- resta: `-`
- multiplicacion: `*`
- division: `/`
- modulo: `%`
- potencia: `**`
- division entera: `//`

sumar elementos +=
restar elementos -=
multiplicar elementos *=
dividir elementos /=
modulo elementos %=

funcion Type:
    - La función `type()` se utiliza para obtener el tipo de dato de una variable. Por ejemplo: `type(x)` devolverá `<class 'int'>` si `x` es un número entero.

- forma de descomponer un problema:
    Entrada
    Proceso
    Salida


    OR(||)
        Es verdadero si  alguna de las 2 condiciones es verdadera
        Es falso si ambas condiciones son falsas
    AND(&&)
        Es verdadero si ambas condiciones son verdaderas
        Es falso si alguna de las 2 condiciones es falsa